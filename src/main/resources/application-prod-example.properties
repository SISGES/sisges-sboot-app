# Exemplo de variáveis para produção (Railway, VPS, etc.)
# Copie para o painel do provedor ou use application-local.properties (gitignored).
#
# SISGES_UPLOAD_DIR=/var/sisges/uploads
# SPRING_DATASOURCE_URL=jdbc:postgresql://host:5432/db
# SPRING_DATASOURCE_USERNAME=...
# SPRING_DATASOURCE_PASSWORD=...
# SECURITY_JWT_SECRET_KEY=... (mín. 32 caracteres)
